import styled from "styled-components";

export const ButtonContainer = styled.button`
    background: #0f2446;
    border-radius: 10px;
    position: relative;

    color: #ffff;
    padding: 10px 10px;
    min-width: 120px;
    width: 100%;

    &:hover {
        cursor: pointer;
        opacity: 0.6;
    }
` 